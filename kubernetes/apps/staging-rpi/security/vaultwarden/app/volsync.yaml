---
# MUST MANUALLY ADD CILIUM GLOBAL ANNOTATION TO SERVICE CREATED!
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: vaultwarden-staging
  namespace: security
spec:
  rsyncTLS:
    copyMethod: Direct
    destinationPVC: vaultwarden
