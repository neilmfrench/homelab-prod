---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: sync-test-pvc
spec:
  sourcePVC: volsync-test-data
  syncthing:
    serviceType: ClusterIP
    # List of peers that this ReplicationSource should connect to.
    peers:
      # The Syncthing ID of the peer.
      - ID: MMG7G4Y-JCOTXJF-PEGKNWK-7EPD67L-6KT62WZ-76YKM3T-W2SEOXT-WCLMUAR
        # The address of the peer - this will be used as a data connection.
        address: tcp://10.43.11.253:9210
        # Whether or not the peer should introduce this ReplicationSource to other peers.
        introducer: false
# ---
# # yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/replicationsource_v1alpha1.json
# apiVersion: volsync.backube/v1alpha1
# kind: ReplicationSource
# metadata:
#   name: frigate
#   namespace: default
# spec:
#   sourcePVC: config-frigate-0
#   trigger:
#     schedule: "0 0 * * *"
#   restic:
#     copyMethod: Snapshot
#     pruneIntervalDays: 10
#     repository: frigate-restic-secret
#     cacheCapacity: 2Gi
#     volumeSnapshotClassName: csi-ceph-blockpool
#     storageClassName: ceph-block
#     retain:
#       daily: 10
#       within: 3d
