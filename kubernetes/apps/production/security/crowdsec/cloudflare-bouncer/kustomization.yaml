---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: security
resources:
  - ./cache-pvc.yaml
  - ../../../../base/crowdsec-cloudflare-bouncer
configMapGenerator:
  - name: cloudflare-bouncer-configmap
    files:
      - ./bouncer-config.yaml
patchesStrategicMerge:
  - ./cloudflare-bouncer-values.yaml
