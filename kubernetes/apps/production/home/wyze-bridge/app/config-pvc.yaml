---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wyze-bridge-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: ceph-block
