---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: commute-dashboard-2
spec:
  values:
    controllers:
      commute-dashboard:
        containers:
          app:
            image:
              repository: nginx
              tag: alpine
            resources:
              requests:
                cpu: 10m
                memory: 32Mi
              limits:
                memory: 64Mi
    configMaps:
      dashboard:
        enabled: true
        data:
          index.html: |
            <!DOCTYPE html>
            <html lang="en">
            <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>Commute Dashboard</title>
              <style>
                * { margin: 0; padding: 0; box-sizing: border-box; }

                body {
                  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
                  background: rgb(16, 18, 21);
                  color: rgb(228, 228, 231);
                  height: 100vh;
                  overflow: hidden;
                }

                #map {
                  width: 100%;
                  height: 100vh;
                }

                #map .gm-style-cc,
                #map a[href^="https://maps.google.com"] { display: none !important; }

                .info-panel {
                  position: absolute;
                  top: 16px;
                  right: 16px;
                  background: rgb(35, 36, 43);
                  border-radius: 12px;
                  padding: 14px 18px;
                  box-shadow: 0 3px 8px 0 rgba(0, 0, 0, 0.12);
                }

                .drive-time {
                  font-size: 28px;
                  font-weight: 600;
                  color: rgb(228, 228, 231);
                  display: flex;
                  align-items: center;
                  gap: 8px;
                }

                .drive-time .dot {
                  width: 10px;
                  height: 10px;
                  border-radius: 50%;
                  background: rgb(118, 214, 152);
                }

                .drive-time.slow .dot { background: rgb(255, 219, 117); }
                .drive-time.heavy .dot { background: rgb(234, 114, 135); }

                .destination {
                  font-size: 14px;
                  color: rgb(138, 140, 153);
                  margin-top: 4px;
                }

                .route-info {
                  font-size: 12px;
                  color: rgb(138, 140, 153);
                  margin-top: 8px;
                  padding-top: 8px;
                  border-top: 1px solid rgb(46, 48, 56);
                }

                .updated {
                  font-size: 10px;
                  color: rgb(138, 140, 153);
                  margin-top: 6px;
                }
              </style>
            </head>
            <body>
              <div id="map"></div>
              <div class="info-panel" id="info-panel" style="display: none;">
                <div id="commute-info">
                  <div class="drive-time" id="drive-time">
                    <span class="dot"></span>
                    <span id="duration">--</span>
                  </div>
                  <div class="destination" id="destination">to Office</div>
                  <div class="route-info" id="route-info">-- via --</div>
                  <div class="updated" id="updated">Updated --:--</div>
                </div>
              </div>

              <script>
                const CONFIG = {
                  home: { lat: parseFloat('${SECRET_HOME_LAT}'), lng: parseFloat('${SECRET_HOME_LNG}') },
                  office: { lat: parseFloat('${SECRET_OFFICE_2_LAT}'), lng: parseFloat('${SECRET_OFFICE_2_LNG}') },
                  apiKey: '${SECRET_GOOGLE_MAPS_API_KEY}',
                  commuteStart: 7,
                  commuteEnd: 13,
                  refreshInterval: 5 * 60 * 1000
                };

                let map;
                let routePolylines = [];
                let markers = [];

                const mapStyles = [
                  { elementType: "geometry", stylers: [{ color: "#1d2c4d" }] },
                  { elementType: "labels.text.fill", stylers: [{ color: "#8ec3b9" }] },
                  { elementType: "labels.text.stroke", stylers: [{ color: "#1a3646" }] },
                  { featureType: "road", elementType: "geometry", stylers: [{ color: "#304a7d" }] },
                  { featureType: "road", elementType: "geometry.stroke", stylers: [{ color: "#255763" }] },
                  { featureType: "road.highway", elementType: "geometry", stylers: [{ color: "#2c6675" }] },
                  { featureType: "road.highway", elementType: "geometry.stroke", stylers: [{ color: "#255763" }] },
                  { featureType: "water", elementType: "geometry", stylers: [{ color: "#0e1626" }] },
                  { featureType: "poi", stylers: [{ visibility: "off" }] },
                  { featureType: "transit", stylers: [{ visibility: "off" }] }
                ];

                function initMap() {
                  map = new google.maps.Map(document.getElementById("map"), {
                    zoom: 11,
                    center: CONFIG.home,
                    styles: mapStyles,
                    disableDefaultUI: true,
                    zoomControl: false,
                    mapTypeControl: false,
                    streetViewControl: false,
                    fullscreenControl: false
                  });

                  updateRoute();
                  setInterval(updateRoute, CONFIG.refreshInterval);
                }

                function isCommuteWindow() {
                  const hour = new Date().getHours();
                  return hour >= CONFIG.commuteStart && hour < CONFIG.commuteEnd;
                }

                function clearRoute() {
                  routePolylines.forEach(p => p.setMap(null));
                  routePolylines = [];
                  markers.forEach(m => m.setMap(null));
                  markers = [];
                }

                async function fetchRoute(origin, destination) {
                  const response = await fetch('https://routes.googleapis.com/directions/v2:computeRoutes', {
                    method: 'POST',
                    headers: {
                      'Content-Type': 'application/json',
                      'X-Goog-Api-Key': CONFIG.apiKey,
                      'X-Goog-FieldMask': 'routes.duration,routes.distanceMeters,routes.polyline.encodedPolyline,routes.travelAdvisory.speedReadingIntervals,routes.legs.steps.navigationInstruction'
                    },
                    body: JSON.stringify({
                      origin: { location: { latLng: { latitude: origin.lat, longitude: origin.lng } } },
                      destination: { location: { latLng: { latitude: destination.lat, longitude: destination.lng } } },
                      travelMode: 'DRIVE',
                      routingPreference: 'TRAFFIC_AWARE',
                      extraComputations: ['TRAFFIC_ON_POLYLINE']
                    })
                  });
                  return response.json();
                }

                function getTrafficColor(speed) {
                  switch (speed) {
                    case 'NORMAL': return '#22c55e';
                    case 'SLOW': return '#facc15';
                    case 'TRAFFIC_JAM': return '#ef4444';
                    default: return '#f97316';
                  }
                }

                function displayRoute(route, origin, destination) {
                  clearRoute();

                  const path = google.maps.geometry.encoding.decodePath(route.polyline.encodedPolyline);
                  const speedIntervals = route.travelAdvisory?.speedReadingIntervals || [];

                  const outline = new google.maps.Polyline({
                    path: path,
                    strokeColor: '#1e293b',
                    strokeWeight: 10,
                    strokeOpacity: 1,
                    zIndex: 1,
                    map: map
                  });
                  routePolylines.push(outline);

                  if (speedIntervals.length === 0) {
                    const polyline = new google.maps.Polyline({
                      path: path,
                      strokeColor: '#f97316',
                      strokeWeight: 6,
                      strokeOpacity: 1,
                      zIndex: 2,
                      map: map
                    });
                    routePolylines.push(polyline);
                  } else {
                    speedIntervals.forEach(interval => {
                      const start = interval.startPolylinePointIndex || 0;
                      const end = (interval.endPolylinePointIndex || path.length - 1) + 1;
                      const segmentPath = path.slice(start, end);
                      const color = getTrafficColor(interval.speed);

                      const polyline = new google.maps.Polyline({
                        path: segmentPath,
                        strokeColor: color,
                        strokeWeight: 6,
                        strokeOpacity: 1,
                        zIndex: 2,
                        map: map
                      });
                      routePolylines.push(polyline);
                    });
                  }

                  const originMarker = new google.maps.Marker({
                    position: origin,
                    map: map,
                    icon: {
                      path: google.maps.SymbolPath.CIRCLE,
                      scale: 8,
                      fillColor: '#76d698',
                      fillOpacity: 1,
                      strokeColor: '#fff',
                      strokeWeight: 2
                    }
                  });
                  markers.push(originMarker);

                  const destMarker = new google.maps.Marker({
                    position: destination,
                    map: map,
                    icon: {
                      path: google.maps.SymbolPath.CIRCLE,
                      scale: 8,
                      fillColor: '#ea7287',
                      fillOpacity: 1,
                      strokeColor: '#fff',
                      strokeWeight: 2
                    }
                  });
                  markers.push(destMarker);

                  const bounds = new google.maps.LatLngBounds();
                  path.forEach(point => bounds.extend(point));
                  map.fitBounds(bounds, 60);
                }

                async function updateRoute() {
                  if (!isCommuteWindow()) {
                    document.getElementById('info-panel').style.display = 'none';
                    clearRoute();
                    return;
                  }

                  document.getElementById('info-panel').style.display = 'block';

                  try {
                    const data = await fetchRoute(CONFIG.home, CONFIG.office);

                    if (data.routes && data.routes.length > 0) {
                      const route = data.routes[0];
                      displayRoute(route, CONFIG.home, CONFIG.office);

                      const durationSecs = parseInt(route.duration.replace('s', ''));
                      const durationMins = Math.round(durationSecs / 60);
                      const distanceMiles = (route.distanceMeters / 1609.34).toFixed(1);

                      document.getElementById('duration').textContent = durationMins + ' min';
                      document.getElementById('route-info').textContent = distanceMiles + ' mi';

                      const now = new Date();
                      document.getElementById('updated').textContent =
                        'Updated ' + now.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' });
                    }
                  } catch (error) {
                    console.error('Route fetch error:', error);
                  }
                }
              </script>
              <script async defer
                src="https://maps.googleapis.com/maps/api/js?key=${SECRET_GOOGLE_MAPS_API_KEY}&libraries=geometry&callback=initMap">
              </script>
            </body>
            </html>
    persistence:
      dashboard:
        enabled: true
        type: configMap
        name: commute-dashboard-2
        globalMounts:
          - path: /usr/share/nginx/html
    service:
      app:
        controller: commute-dashboard
        ports:
          http:
            port: 80
    ingress:
      app:
        enabled: true
        className: traefik
        hosts:
          - host: &host "commute-2.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: app
                  port: http
