---
apiVersion: k8s.mariadb.com/v1alpha1
kind: MariaDB
metadata:
  name: mariadb
spec:
  rootPasswordSecretKeyRef:
    name: firefly-iii-mariadb-root-password
    key: PASSWORD
  username: firefly
  passwordSecretKeyRef:
    name: firefly-iii-mariadb-root-password
    key: PASSWORD
  database: fireflydb
  storage:
    size: 12Gi
