---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dependency-track
spec:
  interval: 15m
  chart:
    spec:
      # renovate: registryUrl=https://dependencytrack.github.io/helm-charts
      chart: dependency-track
      version: 0.46.0
      sourceRef:
        kind: HelmRepository
        name: dependency-track
        namespace: flux-system
  maxHistory: 3
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
