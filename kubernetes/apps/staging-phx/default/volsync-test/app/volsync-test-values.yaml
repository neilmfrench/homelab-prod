---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: volsync-test
spec:
  values:
    image:
      repository: debian
      tag: "latest"
    command: ["sleep", "infinity"]
    persistence:
      data:
        enabled: true
        existingClaim: volsync-test-data
        mountPath: /mnt/data
