---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: security
resources:
  - ./data-pvc.yaml
  # backup to home
  # - ./restic-config.sops.yaml
  # backup to OCI PHX
  # - ./oci-phx-restic-config.sops.yaml
  # clone to staging
  - ./dummy-volsync-dest-svc.yaml
  - ./staging-replicationdestination-key.sops.yaml
  # volsync
  - ./volsync.yaml
  # app
  - ../../../../base/vaultwarden
patches:
  - path: ./vaultwarden-values.yaml
