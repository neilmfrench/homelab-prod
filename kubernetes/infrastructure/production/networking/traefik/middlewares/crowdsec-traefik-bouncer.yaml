apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: crowdsec-traefik-bouncer
  namespace: networking
spec:
  forwardAuth:
    address: http://crowdsec-traefik-bouncer.security/api/v1/forwardAuth
    trustForwardHeader: true
