---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scrutiny-web-config
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
