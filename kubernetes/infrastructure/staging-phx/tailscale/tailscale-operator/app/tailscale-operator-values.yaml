---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
spec:
  values:
    installCRDs: true
    oauth:
      clientId: "${SECRET_TAILSCALE_OAUTH_CLIENT_ID}"
      clientSecret: "${SECRET_TAILSCALE_OAUTH_CLIENT_SECRET}"
    operatorConfig:
      defaultTags:
        - tag:k8s-operator-staging-phx
      hostname: k8s-operator-staging-phx
      logging: info
    proxyConfig:
      defaultTags: tag:k8s-staging-phx
