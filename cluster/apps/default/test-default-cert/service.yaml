---
apiVersion: v1
kind: Service
metadata:
  name: default-cert-test
  namespace: development
spec:
  selector:
    app: default-cert-test
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
