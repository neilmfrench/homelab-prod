---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-hue-bridge-config
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 800Mi
  storageClassName: longhorn
