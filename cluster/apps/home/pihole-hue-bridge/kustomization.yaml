---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - config-pvc.yaml
  - deployment.yaml
  - resource-quota.yaml
namespace: home
configMapGenerator:
  - name: ha-bridge-config
    files:
      - ha-bridge.config
generatorOptions:
  disableNameSuffixHash: true
