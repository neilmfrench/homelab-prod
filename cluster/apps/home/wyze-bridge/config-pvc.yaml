---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wyze-bridge-config
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: longhorn
