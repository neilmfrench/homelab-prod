---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ghost-config
  namespace: magazine
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
